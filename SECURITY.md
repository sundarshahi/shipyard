# Security Policy

Drydock ships security-by-default behavior into the projects it builds, and holds its own codebase to the same standard.

## Supported versions

The latest released version on `main` receives security fixes. Older versions are not maintained.

| Version | Supported |
|---|---|
| 2.2.x   | Yes |
| < 2.2   | No  |

## Reporting a vulnerability

Please report security issues **privately** — do not open a public issue for an exploitable vulnerability.

- Preferred: open a [GitHub Security Advisory](https://github.com/sundarshahi/drydock/security/advisories/new) on the repository.
- Alternative: email **shahithakurisundar@gmail.com** with the details.

Include:

- Affected file(s) or component (e.g. a hook script, a skill, the plugin manifest).
- Reproduction steps or a proof of concept.
- Impact assessment (what an attacker could do).

We aim to acknowledge reports within a few business days and to coordinate a fix and disclosure timeline with you. We follow responsible-disclosure practices and will credit reporters who request it.

## Scope

Installing Drydock registers hooks and bundled scripts that **execute in your Claude Code session and on your machine**, so the executable surface is what matters most. In scope:

- **Hook scripts that run in your session**, and their wiring in `hooks/hooks.json`:
  - `hooks/secret-guard.sh` — `PreToolUse` on `Write`/`Edit`/`MultiEdit`/`NotebookEdit` and `Bash`.
  - `hooks/session-guard.sh` — `SessionStart`.
- **Bundled orchestrator scripts that run on your machine** — `skills/drydock/scripts/bootstrap-workspace.sh`, `verify-gate.py`, and `aggregate-cost.py`.
- **The plugin manifest, skills, shared protocols, and templates** — the content that drives agent behavior (e.g. an instruction that could cause an agent to take an unsafe action).

Out of scope:

- Vulnerabilities in projects *generated by* Drydock — report those to the generated project's owner.
- Claude Code itself, and any third-party tools the pipeline invokes (semgrep, trivy, OWASP ZAP, etc.) — report those upstream.

## Handling secrets

Drydock's `secret-guard` hook blocks writing/committing secret files and scans staged diffs for credentials. Never commit real secrets to this repository. If a secret is exposed, rotate it immediately and report it via the channels above.
